-- 1.	Show all customers whose last names start with T. Order them by first name from A-Z.
SELECT first_name, last_name
FROM customer
WHERE last_name LIKE 'H%'
ORDER BY first_name;
--selcting first names and last names from the customer table where the last name begins with H and ordering alphabetically by frist name.

-- 2.	Show all rentals returned from 5/28/2005 to 6/1/2005
SELECT *
FROM rental
WHERE return_date
BETWEEN '2005-05-28' AND '2005-06-01';
-- selecting all columns from the rental table where the return date is between the given dates.

-- 3.	How would you determine which movies are rented the most?
SELECT *
FROM inventory
LIMIT 10;

SELECT *
FROM rental
LIMIT 10;
